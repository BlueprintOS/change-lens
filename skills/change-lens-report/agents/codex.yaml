interface:
  display_name: "Change Lens Report"
  short_description: "Generate and compare code view reports"
  default_prompt: "Use $change-lens-report to generate or update a project code view."
compatibility:
  cli: "codex"
  install_path: "${CODEX_HOME:-~/.codex}/skills/change-lens-report"
  note: "Codex-compatible SKILL.md layout; install the whole skill folder."
usage:
  trigger: "$change-lens-* or /change-lens-* when supported by the active Codex surface"
  rule: "Use the same SKILL.md contract as Claude and OpenCode; do not rely on Codex-only behavior."
