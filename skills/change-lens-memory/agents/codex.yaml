interface:
  display_name: "Change Lens Memory"
  short_description: "Record and reuse change-lens pitfalls"
  default_prompt: "Use $change-lens-memory to record or list lessons from change-lens usage."
compatibility:
  cli: "codex"
  install_path: "${CODEX_HOME:-~/.codex}/skills/change-lens-memory"
  note: "Codex-compatible SKILL.md layout; install the whole skill folder."
usage:
  trigger: "$change-lens-* or /change-lens-* when supported by the active Codex surface"
  rule: "Use the same SKILL.md contract as Claude and OpenCode; do not rely on Codex-only behavior."
