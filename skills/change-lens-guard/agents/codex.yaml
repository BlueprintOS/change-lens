interface:
  display_name: "Change Lens Guard"
  short_description: "Guardrails for scoped code changes"
  default_prompt: "Use $change-lens-guard to lock and audit a scoped production change."
compatibility:
  cli: "codex"
  install_path: "${CODEX_HOME:-~/.codex}/skills/change-lens-guard"
  note: "Codex-compatible SKILL.md layout; install the whole skill folder."
usage:
  trigger: "$change-lens-* or /change-lens-* when supported by the active Codex surface"
  rule: "Use the same SKILL.md contract as Claude and OpenCode; do not rely on Codex-only behavior."
