interface:
  display_name: "Change Lens Locate"
  short_description: "Read-only code location commands"
  default_prompt: "Use $change-lens-locate when working with change-lens workflows."
compatibility:
  cli: "claude"
  install_path: "${CLAUDE_HOME:-~/.claude}/skills/change-lens-locate"
  note: "Claude-compatible SKILL.md layout; install the whole skill folder."
usage:
  trigger: "$change-lens-locate"
  rule: "Use only for explicit /change-lens-locate commands."
